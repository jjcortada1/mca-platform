node_modules
.next
.env
.env.local
*.log
.DS_Store
.replit-cache
.setup-complete

# TypeScript incremental build cache
*.tsbuildinfo

# Replit upload scratch — snapshots and pasted files, never part of the source
attached_assets/
